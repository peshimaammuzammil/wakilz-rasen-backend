# ── Core framework ────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-dotenv==1.0.1

# ── Logging ───────────────────────────────────────────────────────────────────
loguru==0.7.2

# ── WebRTC (server-side peer connection + audio codec) ────────────────────────
aiortc==1.9.0
# PyAV (av) is a transitive dependency of aiortc — version resolved automatically

# ── WebSocket (Rasen media connection) ────────────────────────────────────────
websockets==13.1

# ── HTTP clients ──────────────────────────────────────────────────────────────
httpx==0.27.2

# ── Auth (JWT) ────────────────────────────────────────────────────────────────
PyJWT==2.9.0

# ── Firebase / Firestore (uses ADC on Cloud Run — no JSON key needed) ─────────
google-cloud-firestore==2.19.0
google-auth==2.35.0

# ── Pydantic (FastAPI request/response validation) ────────────────────────────
pydantic==2.9.2
